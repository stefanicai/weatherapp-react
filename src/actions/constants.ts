// I'd normally add a reducer for this info, so you can configure them per environment.

// Another option would be to use environment variables. The disadvantage of that is that if you need
// to build the entire bundle for each environment, so it can slow the build down.
export const API_KEY = '5acbfe9fab6360c14f4fce70d905dbbd'
export const API_HOST = 'https://api.openweathermap.org/data/2.5'